legacy-peer-deps=true

# For fetching packages set your Gitlab authentication token via the following command:
# npm config set "//gitlab.com/api/v4/packages/npm/:_authToken" "gitlab_npm_registry_token"
# where gitlab_npm_registry_token is your unique API token on Gitlab. It can also be an env variable, e.g. ${GITLAB_NPM_REGISTRY_AUTH_TOKEN}
@volterra:registry=https://gitlab.com/api/v4/packages/npm/
