import { CodeSnippet } from '@site/src/components/CodeSnippet'
import { generateXYSnippets } from '@site/src/utils/snippet-generator'

# Test
<CodeSnippet {...generateXYSnippets([1, 2, 3], [{ name: 'Line', x: (d, i) => i, y: d => d }])} />
