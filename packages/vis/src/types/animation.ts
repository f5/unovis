// Copyright (c) Volterra, Inc. All rights reserved.

export enum ExitTransitionType {
  DEFAULT = 'default',
  TO_ANCESTOR = 'to ancestor',
}

export enum EnterTransitionType {
  DEFAULT = 'default',
  FROM_ANCESTOR = 'from ancestor',
}
