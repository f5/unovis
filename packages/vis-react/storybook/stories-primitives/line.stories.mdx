import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { VisXYContainer } from '@volterra/vis-react/containers/xy-container'
import { VisLine } from '@volterra/vis-react/components/line'

import { data } from '../data/time-series'

<Meta title="XY Primitives / Line ✍️" component={VisLine} />

###### ✍️ This story is Work in Progress

# Line

The example below shows you the most basic Line configuration:

<Canvas>
  <Story name="Basic Line">
    <VisXYContainer data={data}>
      <VisLine x={d => d.x} y={[d => d.y, d => d.y1, d => d.y2]} />
    </VisXYContainer>
  </Story>
</Canvas>

### Component Props
<ArgsTable of={VisLine} />
