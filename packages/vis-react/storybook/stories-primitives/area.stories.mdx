import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { VisXYContainer } from '@volterra/vis-react/containers/xy-container'
import { VisArea } from '@volterra/vis-react/components/area'

import { data } from '../data/time-series'

<Meta title="XY Primitives / Area ✍️" component={VisArea} />

###### ✍️ This story is Work in Progress

# Area

The example below shows you the most basic Area configuration:

<Canvas>
  <Story name="Basic Area">
    <VisXYContainer data={data}>
      <VisArea x={d => d.x} y={[d => d.y, d => d.y1, d => d.y2]} />
    </VisXYContainer>
  </Story>
</Canvas>

### Component Props
<ArgsTable of={VisArea} />
