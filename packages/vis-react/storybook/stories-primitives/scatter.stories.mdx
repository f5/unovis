import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { VisXYContainer } from '@volterra/vis-react/containers/xy-container'
import { VisScatter } from '@volterra/vis-react/components/scatter'

import { data } from '../data/time-series'

<Meta title="XY Primitives / Scatter ✍️" component={VisScatter} />

###### ✍️ This story is Work in Progress

# Scatter

The example below shows you the most basic Scatter configuration:

<Canvas>
  <Story name="Basic Scatter">
    <VisXYContainer data={data}>
      <VisScatter x={d => d.x} y={d => d.y} />
    </VisXYContainer>
  </Story>
</Canvas>

### Component Props
<ArgsTable of={VisScatter} />
