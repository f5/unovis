import { moduleMetadata, componentWrapperDecorator } from '@storybook/angular'
import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { VisSingleContainerModule, VisDonutModule } from 'src/public-api'

import { VisDonutComponent } from 'src/components/donut/donut.component'

import { singleVisContentWrapper } from '../../utils/single-content-wrapper'

<Meta
  title="Misc / Donut ✍️"
  component={VisDonutComponent}
  decorators={[
    moduleMetadata({ imports: [VisDonutModule, VisSingleContainerModule] }),
    componentWrapperDecorator(singleVisContentWrapper)
  ]}
/>

<style>{``}</style>

###### ✍️ This story is Work in Progress

# Donut

### Component Inputs
<ArgsTable of={VisDonutComponent} />

