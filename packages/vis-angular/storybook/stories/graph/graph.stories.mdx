import { moduleMetadata, componentWrapperDecorator } from '@storybook/angular'
import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { VisSingleContainerModule, VisGraphModule } from '../../../src/public-api'

import { VisGraphComponent } from '../../../src/components/graph/graph.component'

import { singleVisContentWrapper } from '../../utils/single-content-wrapper'

<Meta
  title="Networks and Flows/Graph ✍️"
  component={VisGraphComponent}
  decorators={[
    moduleMetadata({ imports: [VisGraphModule, VisSingleContainerModule] }),
    componentWrapperDecorator(singleVisContentWrapper)
  ]}
/>

<style>{``}</style>

###### ✍️ This story is Work in Progress

# Graph

### Component Inputs
<ArgsTable of={VisGraphComponent} />



