import { moduleMetadata, componentWrapperDecorator } from '@storybook/angular'
import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { VisLeafletFlowMapModule } from 'src/public-api'

import { VisLeafletFlowMapComponent } from 'src/html-components/leaflet-flow-map/leaflet-flow-map.component'

import { htmlVisContentWrapper } from '../../utils/html-content-wrapper'

<Meta
  title="Maps/Leaflet Flow Map ✍️"
  component={VisLeafletFlowMapComponent}
  decorators={[
    moduleMetadata({ imports: [VisLeafletFlowMapModule] }),
    componentWrapperDecorator(htmlVisContentWrapper)
  ]}
/>

<style>{``}</style>

###### ✍️ This story is Work in Progress

# Leaflet Flow Map
<ArgsTable of={VisLeafletFlowMapComponent} />

