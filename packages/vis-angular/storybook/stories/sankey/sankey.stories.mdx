import { moduleMetadata, componentWrapperDecorator } from '@storybook/angular'
import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { VisSingleContainerModule, VisSankeyModule } from 'src/public-api'

import { VisSankeyComponent } from 'src/components/sankey/sankey.component'

import { singleVisContentWrapper } from '../../utils/single-content-wrapper'

<Meta
  title="Networks and Flows/Sankey ✍️"
  component={VisSankeyComponent}
  decorators={[
    moduleMetadata({ imports: [VisSingleContainerModule, VisSankeyModule] }),
    componentWrapperDecorator(singleVisContentWrapper)
  ]}
/>

<style>{``}</style>

###### ✍️ This story is Work in Progress

# Sankey Diagram
<ArgsTable of={VisSankeyComponent} />
