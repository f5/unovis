import { moduleMetadata, componentWrapperDecorator } from '@storybook/angular'
import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { VisLeafletMapModule } from 'src/public-api'

import { VisLeafletMapComponent } from 'src/html-components/leaflet-map/leaflet-map.component'

import { htmlVisContentWrapper } from '../../utils/html-content-wrapper'

<Meta
  title="Maps/Leaflet Map ✍️"
  component={VisLeafletMapComponent}
  decorators={[
    moduleMetadata({ imports: [VisLeafletMapModule] }),
    componentWrapperDecorator(htmlVisContentWrapper)
  ]}
/>

<style>{``}</style>

###### ✍️ This story is Work in Progress

# Leaflet Map
<ArgsTable of={VisLeafletMapComponent} />

